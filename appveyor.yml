image: Visual Studio 2017

before_build:
 - dotnet --version
 - dotnet restore ./RateLimiter/RateLimiter.csproj --verbosity m
 - dotnet restore ./RateLimiter.Tests/RateLimiter.Tests.csproj --verbosity m
 - nuget restore
 - choco install opencover.portable

build_script:
 - dotnet build "RateLimiter.sln"

test_script:
 - OpenCover.Console.exe -register:user -target:"C:/Program Files/dotnet/dotnet.exe" -targetargs:"test RateLimiter.Tests\RateLimiter.Tests.csproj" -filter:"+[RateLimiter*]* -[RateLimiter.Test*]*" -output:".\coverage.xml" -oldstyle
# use codecov bash script. Requires powershell and bash to be installed, which both are on the AppVeyor machine image

after_test:
  - ps: |
      $env:PATH = 'C:\msys64\usr\bin;' + $env:PATH
      Invoke-WebRequest -Uri 'https://codecov.io/bash' -OutFile codecov.sh
      bash codecov.sh -f "coverage.xml"